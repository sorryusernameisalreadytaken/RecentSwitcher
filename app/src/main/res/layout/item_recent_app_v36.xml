<?xml version="1.0" encoding="utf-8"?>
<!--
    Variant 36: Dual‑focus with explicit nextFocus navigation.

    This variant retains the non‑focusable row but makes both the left
    container (`app_cell`) and the gear button focusable.  The left
    container uses `nextFocusRight` to move to the gear, while the gear
    defines `nextFocusLeft/Up/Down` back to the app_cell.  This layout is
    similar to v33 but does not wrap the label in a vertical layout, making
    it slightly more compact.  Descendant focusability is BEFORE_DESCENDANTS
    so that DPAD will consider the child views first.  Use this variant to
    test a compact dual‑focus approach.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="8dp"
    android:focusable="false"
    android:focusableInTouchMode="false"
    android:descendantFocusability="beforeDescendants">

    <!-- Focusable left container: icon + text on one line -->
    <LinearLayout
        android:id="@+id/app_cell"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:nextFocusRight="@id/settings_button">

        <ImageView
            android:id="@+id/app_icon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="center_vertical"
            android:contentDescription="@string/app_icon_desc" />

        <TextView
            android:id="@+id/app_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:textAppearance="@style/TextAppearance.AppCompat.Body1" />

    </LinearLayout>

    <!-- Focusable gear: nextFocus routes back to app_cell -->
    <ImageButton
        android:id="@+id/settings_button"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="center_vertical"
        android:background="?attr/selectableItemBackground"
        android:contentDescription="@string/open_settings_description"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:padding="4dp"
        android:src="@android:drawable/ic_menu_manage"
        android:nextFocusLeft="@id/app_cell"
        android:nextFocusUp="@id/app_cell"
        android:nextFocusDown="@id/app_cell" />

</LinearLayout>
