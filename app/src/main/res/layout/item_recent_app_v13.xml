<?xml version="1.0" encoding="utf-8"?>
<!--
    Variant 13 of the recent app list item. This variant arranges the row
    into two columns similar to a table: the left column contains the
    application icon and its label, while the right column contains a
    dedicated action button (settings gear). The row and the gear are both
    focusable. Descendant focusability is set to BEFORE_DESCENDANTS so
    that the row receives focus first and DPAD‑RIGHT can move to the gear.
    The left column uses layout_weight to occupy the remaining width so
    that the gear button aligns neatly on the right. DPAD‑DOWN from the
    gear moves to the next row (handled in code).
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="8dp"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:descendantFocusability="beforeDescendants">

    <!-- Left column containing the icon and label -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/app_icon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="center_vertical"
            android:contentDescription="@string/app_icon_desc" />

        <TextView
            android:id="@+id/app_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:textAppearance="@style/TextAppearance.AppCompat.Body1" />

    </LinearLayout>

    <!-- Right column containing the settings gear -->
    <ImageButton
        android:id="@+id/settings_button"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="center_vertical"
        android:background="?attr/selectableItemBackground"
        android:contentDescription="@string/open_settings_description"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:padding="8dp"
        android:src="@android:drawable/ic_menu_manage" />

</LinearLayout>