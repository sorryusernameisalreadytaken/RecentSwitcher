<?xml version="1.0" encoding="utf-8"?>
<!--
    Variant 34: Row‑anchored focus with after‑descendants ordering.

    This variant experiments with a row that itself takes focus first.  The
    root view (`row_root`) is focusable and uses AFTER_DESCENDANTS focus
    ordering so that DPAD navigation will consider child views before
    returning to the row.  DPAD‑RIGHT from the row moves to the gear.  The
    left container (`app_cell`) is not focusable; instead, the row itself
    acts as the focus anchor.  The gear is focusable and its
    `nextFocusLeft/Up/Down` attributes point back to the row so that the
    ListView key handler can intercept DPAD‑UP/DOWN to change rows.  This
    layout tests a best‑practice approach where the row, rather than the
    left container, owns the focus and DPAD‑left returns to the row.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/row_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="8dp"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:descendantFocusability="afterDescendants"
    android:nextFocusRight="@id/settings_button">

    <!-- Left container: not focusable; content only -->
    <LinearLayout
        android:id="@+id/app_cell"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:focusable="false"
        android:focusableInTouchMode="false">

        <ImageView
            android:id="@+id/app_icon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="center_vertical"
            android:contentDescription="@string/app_icon_desc" />

        <TextView
            android:id="@+id/app_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:textAppearance="@style/TextAppearance.AppCompat.Body1" />

    </LinearLayout>

    <!-- Settings gear: focusable.  Returns focus back to the row on UP/DOWN/LEFT. -->
    <ImageButton
        android:id="@+id/settings_button"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="center_vertical"
        android:background="?attr/selectableItemBackground"
        android:contentDescription="@string/open_settings_description"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:padding="4dp"
        android:src="@android:drawable/ic_menu_manage"
        android:nextFocusLeft="@id/row_root"
        android:nextFocusUp="@id/row_root"
        android:nextFocusDown="@id/row_root" />

</LinearLayout>
