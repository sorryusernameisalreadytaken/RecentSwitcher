<?xml version="1.0" encoding="utf-8"?>
<!--
    Variant 9 of the recent app list item. This variant extends the ideas of
    variant 2 by keeping both the list row and the settings gear focusable
    while using BEFORE_DESCENDANTS focus order. The row itself is
    selectable via DPAD navigation and DPAD‑RIGHT will move focus to the
    gear, which in turn allows invoking the app’s system settings. DPAD‑DOWN
    from the gear moves to the next app’s row (handled in code). Use this
    layout if you wish to prioritise the list item for focus but still
    allow explicit navigation to the gear control.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="8dp"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:descendantFocusability="beforeDescendants">

    <ImageView
        android:id="@+id/app_icon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="center_vertical"
        android:contentDescription="@string/app_icon_desc" />

    <TextView
        android:id="@+id/app_text"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="12dp"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1" />

    <ImageButton
        android:id="@+id/settings_button"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="center_vertical"
        android:background="?attr/selectableItemBackground"
        android:contentDescription="@string/open_settings_description"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:padding="4dp"
        android:src="@android:drawable/ic_menu_manage" />

</LinearLayout>